# VTON Demo Explainer Video — Production Prompt + Brief

**Demo:** Virtual Lens Try-On (`/demo/goggle-vton` · embed at [dev.colaberry.ai/demo/lens](https://dev.colaberry.ai/demo/lens))
**Intent:** A 60–90 second **product explainer** that shows the demo in use and explains what it does — NOT a page scroll.
**Deliverable target:** 1920×1080 H.264 MP4, muted-safe, subtitled, under 25 MB.
**Owner:** Karun
**Updated:** 2026-04-16

---

## 1. Why this document exists

Karun's ask was "add a video." The page-tour generator shipping today (`public/videos/goggle-vton-walkthrough.mp4`, generated via Playwright + ffmpeg) fills the slot but doesn't explain the product. This brief defines the **real** explainer and gives you the exact prompts to hand to whichever path you pick.

The demo itself lives in two places:

| Role | URL |
|---|---|
| Details page (slot where the video shows) | `dev.colaberry.ai/demo/goggle-vton` |
| Live interactive app (what we're filming) | `dev.colaberry.ai/demo/lens` (iframes `vton-demo-956818257204.us-east1.run.app`) |

---

## 2. What the video must accomplish

In 60–90 s an enterprise visitor must understand:

1. **What the product does** — virtual eyewear try-on, real-time 3D overlay on a live camera feed, AI face-shape classification, expert-optician style recommendations.
2. **Why it's impressive** — 30–60 FPS overlay, 478-point MediaPipe mesh, Three.js + R3F WebGL rendering, LangGraph + GPT-4.1 style engine, **15 real frame SKUs** spanning 14 silhouettes (Aviator, Wayfarer, Cat Eye, Clubmaster, Pilot, Sport Wrap, Round Tortoise, Oversized, Geometric, Rectangle, Retro, Fashion, Bold Square, Classic), four materials (acetate, metal, plastic, mixed).
3. **That it's real** — not a mock. They can click "Launch live demo" and try it on their own face right now.
4. **What it implies** — Colaberry can ship this class of end-to-end AI product for *their* business.

Secondary: must work on **mute autoplay** (LinkedIn, Slack previews, sales decks). Burned-in captions mandatory.

---

## 3. Three production paths, ranked

### Path A · Compositing: AI B-roll + real screen captures (RECOMMENDED)

The 2026 consensus across Veo 3.1, Sora 2, and Runway Gen-4.5 is that **no general text-to-video model reliably renders accurate software UI** — button labels warp, cursors hallucinate, app chrome drifts frame-to-frame. The working pattern all four top models agree on: generate the **human / environment** shots with AI, record the **product UI** shots for real, composite in DaVinci / Premiere / CapCut.

For us that means:
- **2 AI clips** (Veo 3.1 or Sora 2) for the pain shot (person in mirror with two frames) + the close human shot (someone's face lighting up as frames appear).
- **5 screen-capture clips** of the actual VTON app doing its thing — camera on → mesh overlay → frame swap → tint cycle → face-shape panel + GPT recommendation.
- 2 brand title cards (already auto-generated by our ffmpeg pipeline — see §7).

Cost for AI clips: ~$6 on Veo 3.1 at 1080p × 8 s × 2 generations with retries, or $20/mo Sora included. ElevenLabs v3 voice-over: ~$0.15 for 90 s.

**This is the only path that produces a shippable video — go here first.**

### Path B · Playwright-driven fake-camera screen recording (ship today, zero cost)

Chromium supports `--use-file-for-fake-video-capture` to pipe a `.y4m` video file into `navigator.mediaDevices.getUserMedia()` as the webcam feed. That means we can:

1. Grab a royalty-free close-up face clip (e.g. Pexels, stock creative-commons `.mp4` of someone looking at the camera turning head left-right).
2. Convert it to `.y4m` with ffmpeg.
3. Launch Playwright against `/demo/lens` with those flags so the VTON app sees the stock face as "the user".
4. Script click sequences (enable camera → switch frames → toggle tint → open recommendations) while Playwright records the whole browser viewport to WebM.
5. Concat with title + end cards.

Output: a **real demo recording** of the product working on a real face, fully automated, reproducible on every release. No AI video tool needed. Full extension of our existing `scripts/generate-demo-walkthrough.mjs`.

**Status: shipped (2026-04-16).** The generator now supports `--mode=explainer` and a companion `scripts/prepare-fake-camera.mjs` converts any face clip to the Y4M Chromium needs. See **§14 — Path B usage** for the exact two-step flow.

### Path C · Human shoot (when there's budget + time)

Hire a DP, record the VTON actually being used by a model + voice-over artist in a studio. 1–2 day shoot, 1 week post. This is the gold standard for a prod website. Use §5–§6 below as the creative brief for the agency.

---

## 4. The 75-second shot list (for any path)

Each beat is 5–10 s. Burned subtitles. Same structure for A / B / C.

| # | Duration | Visual | Voice-over (burned caption) |
|---|----------|--------|-----------------------------|
| 1 | 2 s | **Title card.** Zinc-950 background, coral `#DC2626` dot, Inter 68pt: "Virtual Lens Try-On". Small-caps kicker: "VIRTUAL TRY-ON". | *(music bed only)* |
| 2 | 6 s | **AI B-roll or stock footage.** Mid-shot of a person in their late 20s at a dressing-room mirror, two pairs of eyewear in hand, expression uncertain. Zinc-cool mirror lighting, shallow DoF. | "Eyewear buyers drop off when they can't see how a frame will actually fit their face." |
| 3 | 8 s | **Screen capture · camera enable.** Browser opens `/demo/lens`, user allows camera, webcam light on, face appears on canvas, 478-point face mesh grid flickers on for ~0.8 s, then 3D aviator frames snap onto the face. | "Open the demo — live camera, four-seventy-eight-point face mesh, 3D frames locked to your head in under a second." |
| 4 | 6 s | **Screen capture · head-movement tracking.** User turns head left, right, nods; frames track without lag. Lower-third overlay: **30–60 FPS · MediaPipe WASM · Three.js / R3F**. | "The overlay tracks at thirty to sixty frames per second — WebGL in the browser, nothing ships your camera anywhere." |
| 5 | 7 s | **Screen capture · photo mode + frame grid.** Click "Photo mode". Grid of 15 frame thumbnails appears (Aviator Classic, Sport Wrap, Rose Gold, Silver Frame, Bold Square, Round Tortoise, Slim Rectangle, Cat Eye, …). Cursor hovers through 4; each swaps on-face instantly. | "Capture a photo and try fifteen real frames side-by-side — no re-calibration, no waiting." |
| 6 | 8 s | **Screen capture · face-shape analysis.** Split panel: canvas on left with detected face; right panel reads *Face shape: oval · confidence 0.86 · W 142mm · H 195mm · ratio 0.73*. | "Under the hood, we classify face shape from the mesh geometry — oval, round, square, heart, oblong." |
| 7 | 10 s | **Screen capture · GPT-4.1 recommendations.** Right panel flips to "Recommended for oval faces": three ranked frames with one-line rationales ("Balanced browline suits your jaw width", "Slight keyhole bridge elongates vertical proportions"). | "A LangGraph pipeline wrapping GPT-four-point-one plays expert optician — it picks frames for the detected face shape and tells you why." |
| 8 | 6 s | **Screen capture · lens tint + 2D upload.** Tint selector cycles: clear → blue-light → polarized → sunglass → mirror. Cut to a drag-and-drop zone with a 2D frame PNG; SVG overlay generates automatically. | "Preview every lens tint in real time. Enterprise clients upload their own SKU catalog — 2D or 3D — and see it on-face immediately." |
| 9 | 6 s | **Tech stack montage.** Full-bleed zinc-950 card, seven logos slide in 200ms apart: FastAPI, PostgreSQL, LangGraph, MediaPipe, Three.js, Docker, Playwright. | "Production stack: FastAPI, PostgreSQL, LangGraph, Three.js — Docker-orchestrated with end-to-end Playwright tests." |
| 10 | 4 s | **Metrics band.** Four stats animate in (Inter 72pt, zinc-50): *30–60 FPS · 478-point · 15+ frame SKUs · GPT-4.1*. Same layout as the page's hero band. | "Production-grade from frame one." |
| 11 | 8 s | **AI B-roll or studio shot.** Medium-shot of a thoughtful man in his late 30s wearing a charcoal crew-neck, soft zinc-grey backdrop, direct eye-contact. Editorial Stripe/Linear brand-video look. | "If you're in retail, eyewear, apparel, or healthcare — this is what your try-on should look like in 2026. We build these for enterprise in eight weeks." |
| 12 | 3 s | **End card.** Zinc-950, coral dot, Inter: "colaberry.ai · /demo/goggle-vton · Book a demo". | "Visit colaberry dot ay eye. Book a demo." |

**Total:** ~74 s.

---

## 5. Voice-over — clean script (for ElevenLabs v3 or a human VO artist)

### 5.1 Clean reading copy

> Eyewear buyers drop off when they can't see how a frame will actually fit their face.
>
> Open the demo — live camera, four-seventy-eight-point face mesh, 3D frames locked to your head in under a second. The overlay tracks at thirty to sixty frames per second — WebGL in the browser, nothing ships your camera anywhere.
>
> Capture a photo and try fifteen real frames side-by-side — no re-calibration, no waiting. Under the hood, we classify face shape from the mesh geometry — oval, round, square, heart, oblong. A LangGraph pipeline wrapping GPT-four-point-one plays expert optician — it picks frames for the detected face shape and tells you why.
>
> Preview every lens tint in real time. Enterprise clients upload their own SKU catalog — 2D or 3D — and see it on-face immediately.
>
> Production stack: FastAPI, PostgreSQL, LangGraph, Three.js — Docker-orchestrated with end-to-end Playwright tests. Production-grade from frame one.
>
> If you're in retail, eyewear, apparel, or healthcare — this is what your try-on should look like in 2026. We build these for enterprise in eight weeks.
>
> Visit colaberry dot ay eye. Book a demo.

~180 words. At 170 wpm delivery that's ~63 s — leaves 10–12 s of visual-only beats under music bed.

### 5.2 ElevenLabs v3 prompt (paste as-is)

ElevenLabs v3 uses **bracketed audio tags inline** (no SSML). Pauses come from ellipses `…`, em dashes `—`, and `[pause]`. Emphasis comes from capitalization (`FOUR`) or `[emphatic]`. Emotion tags are per-phrase.

Voice: pick "Adam" or "Brian" (male, mid-30s, American, measured) — or clone a C-suite voice from a 60-second sample via Professional Voice Clone ($99/mo tier).

Voice-settings sliders: **Stability 45 · Similarity 75 · Style 20 · Speaker boost ON**. Model: `eleven_multilingual_v2` or the v3 alpha if your account has access.

```
[calm, confident-measured, mid-30s male, American]
Eyewear buyers drop off when they can't see how a frame will actually fit their face.

[slight uptick in energy]
Open the demo — live camera, four-seventy-eight-point face mesh, 3D frames locked to your head in under a second. [pause] The overlay tracks at [emphatic] thirty to sixty frames per second — WebGL in the browser, nothing ships your camera anywhere.

Capture a photo and try [emphatic] fifteen real frames side-by-side — no re-calibration, no waiting. [pause] Under the hood, we classify face shape from the mesh geometry — oval, round, square, heart, oblong. A LangGraph pipeline wrapping GPT-four-point-one plays expert optician — it picks frames for the detected face shape, [pause] and tells you why.

Preview every lens tint in real time. Enterprise clients upload their own SKU catalog — 2D or 3D — and see it on-face immediately.

[measured, authoritative]
Production stack: FastAPI, PostgreSQL, LangGraph, Three.js — Docker-orchestrated with end-to-end Playwright tests. [pause] Production-grade from frame one.

[warmer, addressing-camera]
If you're in retail, eyewear, apparel, or healthcare — this is what your try-on should look like in 2026. [pause] We build these for enterprise in [emphatic] eight weeks.

[slightly slower, closing]
Visit colaberry dot ay eye. [pause] Book a demo.
```

Render each paragraph separately so you can regenerate individual takes without losing the whole file. Target -16 LUFS integrated after mastering.

---

## 6. Text-to-video prompts for the AI B-roll clips (Path A)

For the 2026 landscape you only need **two clips** generated, and both are human/environment shots with no software UI. Pick **Veo 3.1** if you want native synced audio (optional — we're replacing the audio in post anyway); **Sora 2** if you already pay for ChatGPT Pro; **Runway Gen-4.5** if you want the most control for editorial work.

### 6.1 Beat 2 · Pain-statement shot (6 s)

```
[Veo 3.1 / Sora 2 / Runway Gen-4.5]

Subject: A Caucasian woman in her late 20s standing in front of a softly lit
dressing-room mirror, holding one pair of eyewear in each hand — a rounded
tortoiseshell in the left, a thin rectangular wire frame in the right.
Expression: genuinely uncertain, slight head tilt.

Action: She looks between the two frames, lifts the tortoise pair halfway toward
her face, hesitates, lowers it. Shallow focus breathing. No glasses end up on
her face.

Style: Editorial commercial, Stripe-brand mood. Cool zinc-grey palette
(#09090B, #18181B, #A1A1AA — no warm tones, no blue, no green). 35mm look,
shallow depth of field, soft window light from camera-right, natural skin tones.

Camera: Eye-level, slow 3-second push-in from medium to medium-close-up.
Handheld energy at ~8% subtle sway. No whip pans.

Lighting: Low-contrast soft key from camera-right through a diffusion grid.
Practical dressing-room sconce visible but out of focus on left frame.
Negative fill on camera-left for dimensional shadows.

Audio: Ambient room tone only. No dialogue. Very faint mirror/cloth rustle.

Aspect: 16:9 · Resolution: 1080p · Duration: 6s · 30fps

Negative prompt: no branded glasses, no visible logos, no text on signage,
no saturated colors, no golden-hour warmth, no fisheye, no extreme angles,
no stylized grading, no animation feel, no plastic skin.
```

### 6.2 Beat 11 · Authority / framing shot (8 s)

```
[Veo 3.1 / Sora 2 / Runway Gen-4.5]

Subject: An Indian-origin man in his late 30s, clean-shaven, short dark hair,
charcoal crew-neck sweater. Calm, thoughtful, composed. Looking slightly
off-axis from camera then settling direct-to-lens on the final beat.

Action: Minimal movement — a single slow controlled head micro-turn from
3/4 to direct eye-contact over 4 seconds, then held for 4 seconds. Blink
rate natural (~1 blink per 3 seconds).

Style: Editorial enterprise brand video — think Stripe, Linear, Anthropic.
Zinc palette only (charcoal on charcoal background, zinc-50 skin highlight).
No brand markings visible. Soft studio lighting, no practicals. Filmed on
ARRI-look sensor emulation, anamorphic bokeh.

Camera: Medium-shot framing (chest up). Locked-off tripod. 50mm equivalent.

Lighting: Three-point studio. Soft key from camera-left through 4ft octabox,
~3200K corrected to neutral. Fill at 1:4 ratio from camera-right. Subtle
hair light overhead. Background falls to zinc-900 at 2 stops under key.

Audio: Silent. Room tone only.

Aspect: 16:9 · Resolution: 1080p · Duration: 8s · 30fps

Negative prompt: no smile, no hand gestures, no animated expression, no text
overlays, no warm color grading, no blue hues, no green hues, no corporate-
stock-photo aesthetic, no shiny surfaces, no stubble twitch, no over-sharp
skin texture, no uncanny valley.
```

**Iteration advice:** Veo 3.1 and Sora 2 both benefit from a negative prompt block. Generate **3 variations per clip**, pick the best, cut 6–8 s of footage from 8 s of generation. Budget 2 hours for iteration on each clip.

---

## 7. The screen-capture clips (Path A beats 3–8)

These are the **5 real product recordings** that carry the weight of the explainer. Two ways to produce them:

### 7.1 Manual capture (fastest, 30 min)

```bash
# 1. Start the dev VTON backend + frontend
cd vton-demo && docker compose up -d

# 2. In Chrome, open the demo URL
open -a "Google Chrome" https://dev.colaberry.ai/demo/lens

# 3. Capture with the macOS built-in screen recorder
#    (Cmd+Shift+5 → "Record Selected Portion" → 1440x900 window)
#    One take per beat, 8–10 seconds each.
#    Export to: ~/Desktop/vton-raw-{3,4,5,6,7,8}.mov

# 4. Transcode to H.264 1080p for editing
for i in 3 4 5 6 7 8; do
  ffmpeg -i ~/Desktop/vton-raw-$i.mov \
    -vf "scale=1920:1080:force_original_aspect_ratio=decrease,pad=1920:1080:(ow-iw)/2:(oh-ih)/2:color=0x09090B,fps=30" \
    -c:v libx264 -preset veryfast -crf 18 -pix_fmt yuv420p \
    ~/Desktop/vton-clip-$i.mp4
done
```

Do one clean take per beat. Hide the macOS menu bar (Shift-Cmd-F for fullscreen). Use a dark desktop wallpaper. Make sure the dock auto-hides. Patch the camera LED in post if visible.

### 7.2 Fully automated — fake-camera Playwright script (Path B extension)

When you decide to go this route, I'll extend `scripts/generate-demo-walkthrough.mjs` with a second mode: `--mode=explainer`. The key Chromium flags:

```js
const browser = await chromium.launch({
  headless: false,  // needs headful for mediaDevices + WebGL
  args: [
    "--use-fake-ui-for-media-stream",
    "--use-fake-device-for-media-stream",
    `--use-file-for-fake-video-capture=${absolutePathToFakeFaceY4m}`,
    "--autoplay-policy=no-user-gesture-required",
    "--enable-features=VaapiVideoDecoder",
    "--disable-blink-features=AutomationControlled",
  ],
});
```

We'd prepare `public/videos/source/fake-face-rotating.y4m` by converting one royalty-free [Pexels face clip](https://www.pexels.com/search/videos/face%20portrait/) via:

```bash
ffmpeg -i pexels-face-rotating.mp4 -pix_fmt yuv420p -f yuv4mpegpipe \
  public/videos/source/fake-face-rotating.y4m
```

Playwright then scripts the click sequence: `Enable camera` → wait for mesh overlay → `Switch to photo mode` → click through 4 frames → `Open AI recommendations` → `Cycle tints`. Each action has a `page.waitForTimeout(800)` beat to let the UI settle before the next click.

Say the word and this ships as a follow-up task.

---

## 8. Brand + visual lock (NON-NEGOTIABLE)

Every clip, card, and overlay must match [CLAUDE.md § Locked Theming Standard](../CLAUDE.md):

| Rule | Value |
|------|-------|
| Palette | **Zinc scale + coral `#DC2626` only.** No blue, emerald, green, amber, or slate. |
| Background | Zinc-950 `#09090B` (dark-first) |
| Surface / card | Zinc-900 `#18181B` |
| Text primary | Zinc-50 `#FAFAFA` |
| Text muted | Zinc-400 `#A1A1AA` |
| Accent dot / CTA | Coral `#DC2626` light / `#F87171` dark |
| Typography | Inter (display + body). Weights 400 / 600 / 800 only. |
| Lower-third chip | Zinc-900 card, coral 6×6 dot, Inter 18–22pt muted text |
| Motion ease | `cubic-bezier(0.22, 1, 0.36, 1)` — matches site `--ease-entry` |
| Card corner radius | 24px |

**Asset truth:** CSS custom properties in `src/styles/globals.css`. Any agency or AI video tool must mirror these exact hex values — don't eyeball.

---

## 9. Music + sound design

- **Bed:** instrumental, 88–92 BPM, minor key, 4/4 or 6/8 — editorial not corporate. Artlist/Musicbed enterprise license. Avoid "cinematic tech whoosh" libraries.
- **SFX:** one soft UI click on frame-swap moments (beats 5 + 8). No laser whooshes, no risers.
- **Room tone:** record 30 s before + after for edit fills (or grab from Veo/Sora ambient output).
- **Mix:** VO front at -16 LUFS integrated, music bed -22 LUFS, SFX -18 LUFS transient peaks.

---

## 10. Accessibility + AEO

- **Burned-in captions** in post (white 60%-opaque zinc-950 box, Inter 28pt).
- **Caption sidecar** `.vtt` published alongside the MP4 (WCAG 2.2 AA).
- **Transcript hidden block** on `/demo/goggle-vton` below the video — lets GPTBot / ClaudeBot / Perplexity index the spoken content (the whole point of `colaberry.ai`'s AEO stance — see [Site-purpose memory](#)).
- **Schema.org `VideoObject`** added to the detail page's JSON-LD when Path A/C ships:

```ts
// src/pages/demo/[slug].tsx — inside ldJson merge
video: {
  "@type": "VideoObject",
  name: `${demo.title} — Explainer`,
  description: demo.summary,
  thumbnailUrl: buildCanonical(demo.videoPoster!),
  uploadDate: "2026-04-20",          // update at ship
  duration: "PT1M14S",                // ISO 8601
  contentUrl: buildCanonical(demo.videoEmbedUrl!),
  embedUrl: buildCanonical(demo.videoEmbedUrl!),
  publisher: { "@type": "Organization", name: "Colaberry AI" },
}
```

- **No autoplay audio** — the `<video>` tag on `/demo/[slug]` uses `controls preload="metadata"`. Intentional, leave it.

---

## 11. Where the final file goes

After the explainer ships, swap [src/data/demos.ts](../src/data/demos.ts):

```ts
// Current (page-tour placeholder):
videoEmbedUrl: "/videos/goggle-vton-walkthrough.mp4",
videoPoster: "/videos/goggle-vton-walkthrough-poster.jpg",
videoCaption: "22-second automated page tour · generator in scripts/generate-demo-walkthrough.mjs",

// Path A/C (external-host the master MP4):
videoEmbedUrl: "/videos/goggle-vton-explainer.mp4",       // upload your 1080p master here
videoPoster: "/videos/goggle-vton-explainer-poster.jpg",
videoCaption: "Product explainer · 1m 14s",

// Or — if hosting on YouTube for better CDN + captions:
videoEmbedUrl: "https://www.youtube.com/embed/VIDEO_ID",
videoPoster: undefined,                                    // YouTube provides its own
videoCaption: "Product explainer · 1m 14s",
```

Update the `VideoObject` JSON-LD in the detail page the same commit.

---

## 12. Acceptance checklist (lock gates)

- [ ] Runtime 60–120 s
- [ ] All colors within zinc + coral palette — zero forbidden hues
- [ ] Inter font on every overlay
- [ ] Burned captions + `.vtt` sidecar both present
- [ ] VO at -16 LUFS integrated
- [ ] **Works muted** — every information-carrying beat is visible, not spoken-only
- [ ] Poster frame is from a visual-hold moment (title card or a clean face-mesh beat)
- [ ] File under 25 MB for web-served asset
- [ ] Plays in Safari, Chrome, Firefox — macOS, iOS, Android — test before shipping
- [ ] Transcript added to `/demo/goggle-vton` hidden block for AEO
- [ ] `VideoObject` JSON-LD live
- [ ] `npm run build` + `npx tsc --noEmit` + `npm run lint` clean after the swap

---

## 13. Decision table for Karun

| Question | Answer | Go |
|---|---|---|
| Need it this week, zero budget? | **Path B (shipped).** Run the two commands in §14. | Follow §14 |
| Have $50 + 2 hours, want the cleanest edit? | Path A (Veo 3.1 or Sora 2 for 2 B-roll clips, manual screen capture for 5 UI clips, ElevenLabs VO, edit in DaVinci Resolve free) | Follow §4–§7 step-by-step |
| Have a week + budget for an agency? | Path C (hand this doc + shot list + brand lock to the agency) | Treat §4–§10 as the SOW |

---

## 14. Path B — usage (Playwright + fake camera)

Two scripts, two commands. Both live at the repo root under `scripts/`.

**Prereqs (one time):**
- `ffmpeg` in `$PATH` — `brew install ffmpeg` on macOS.
- Playwright Chromium installed — `npx playwright install chromium`.
- A short face clip to use as the fake webcam feed. Any MP4 / MOV / WEBM of a single clearly-visible front-facing face works (smartphone selfie video, CC0 stock from pexels.com / pixabay.com filtered to "people" + "face"). 10–20 seconds is ideal — Chromium auto-loops the file during playback, so a short clip covers the whole recording. Higher-quality input = more convincing overlay.

**Step 1 — convert the face clip to Y4M (Chromium's required fake-camera format):**

```bash
node scripts/prepare-fake-camera.mjs --input ~/path/to/face-clip.mp4
```

Writes `tmp/fake-camera.y4m` (gitignored under `tmp/`). Defaults to 640×480 @ 30fps, 12 s duration — override with `--width`, `--height`, `--fps`, `--duration` if needed.

**Step 2 — record the explainer:**

```bash
node scripts/generate-demo-walkthrough.mjs --mode=explainer
```

Launches Chromium with `--use-file-for-fake-video-capture=tmp/fake-camera.y4m` pointed at the live Cloud Run VTON app, drives it through the architecture-aligned pipeline (detect → classify → fit → recommend → render per `Goggle_VTON_Architecture.pdf §6.1`), then renders `public/videos/goggle-vton-explainer.mp4` + a poster JPG, bracketed by auto-generated title + end cards. Playwright's `recordVideo` captures video only — the MP4 at this stage is muted.

**Step 3 (optional but recommended) — add voice-over:**

```bash
node scripts/add-explainer-audio.mjs
```

Synthesizes a voice-over from `scripts/vo/goggle-vton-explainer.txt` using the built-in macOS `say` command (Samantha, 165 wpm by default), normalizes to broadcast-safe loudness (`loudnorm I=-16 LRA=11 TP=-1.5`), and muxes it into the explainer MP4. The pre-audio file is preserved at `*-explainer-silent.mp4` so re-runs skip the Playwright step. Script copy is grounded in `Goggle_VTON_Architecture.pdf §1` (executive summary) and uses `[[slnc <ms>]]` inline silence markers to land VO beats on the visual pipeline stages.

Override voice or swap in a pre-recorded track:

```bash
node scripts/add-explainer-audio.mjs --voice Daniel --rate 170
node scripts/add-explainer-audio.mjs --audio-file ~/elevenlabs-vo.m4a
```

Total runtime: ~45 s of actual recording + ~20 s ffmpeg stitching. Reproducible on every release.

**Beat structure the Playwright script executes:**

| # | Stage | What the camera sees |
|---|---|---|
| 0 | Init (6 s) | Wait for React SPA mount, WASM load, MediaPipe first frame (covers Cloud Run cold starts) |
| 1 | **DETECT** (5 s) | Live 3D overlay, MediaPipe 478-point face mesh at 30–60 FPS |
| 2 | **CLASSIFY** (~5 s) | Click "Photo Mode" → "Capture photo" — shape classifier runs on the stable frame |
| 3 | **FIT** (~8 s) | Click "Try All Frames" then "Try Another" 3× — trimesh width-scoring variety |
| 4 | **RECOMMEND** (5 s) | Scroll to the LangGraph + GPT-4.1 "Recommended for You" panel |
| 5 | **RENDER** (5 s) | Click "Live Mode" — close on the live Three.js + R3F overlay |

**Wiring the result into the site:** `src/data/demos.ts` already points `videoEmbedUrl` at `/videos/goggle-vton-walkthrough.mp4` (the tour placeholder). When the explainer is ready to ship, swap that to `/videos/goggle-vton-explainer.mp4` (and the matching `videoPoster`) — the detail page at `src/pages/demo/[slug].tsx` detects the extension and renders `<video>` either way.

**Useful flags:**

| Flag | Default | When to use |
|---|---|---|
| `--mode=tour` | `tour` | Page scroll-through of `/demo/[slug]` on local dev server (the original generator) |
| `--mode=explainer` | — | This path (real product in action) |
| `--vton-url <url>` | Cloud Run URL baked in | Point at a staging / preview VTON deployment |
| `--camera-file <path>` | `tmp/fake-camera.y4m` | Use a different Y4M without re-running step 1 |
| `--no-titles` | off | Raw recording only (skip brand title + end cards — useful when splicing into a longer edit) |
| `--headed` | off | Open Chromium with UI for debugging selector drift |

**If a step fails:** re-run with `--headed`. The VTON UI labels the script looks for ("Photo Mode", "Capture photo", "Try All Frames", "Try Another", "Live Mode") are matched with defensive `.or()` chains, but if the VTON app renames a button the corresponding beat will silently hold — the video still concatenates, but that beat shows a static frame. Fix by updating the selector in `recordExplainer()` and re-running.

**Hand-off to Path A / C:** the explainer MP4 is also usable as **source footage** for a human edit. Drop it into DaVinci Resolve, overlay the ElevenLabs VO from §6, trim beats to taste. Path B = the machine baseline; Paths A and C build on it.

---

## Sources (verified 2026-04-16)

- [Veo 3.1 model card — Google DeepMind](https://deepmind.google/models/veo/)
- [Veo 3.1 specs on Vertex AI](https://docs.cloud.google.com/vertex-ai/generative-ai/docs/models/veo/3-1-generate)
- [Sora 2 vs Runway Gen-4 comparison — Medium](https://medium.com/activated-thinker/sora-2-or-runway-gen-4-the-honest-comparison-every-creator-needs-a91cdd3b1ef6)
- [AI Video Model Comparison 2026 — RizzGen](https://rizzgen.ai/blogs/runway-kling-veo-sora-ltx-wan-seedance-comparison)
- [Prompting Eleven v3 (alpha) — ElevenLabs docs](https://elevenlabs.io/docs/best-practices/prompting/eleven-v3)
- [Eleven v3 Audio Tags](https://elevenlabs.io/blog/v3-audiotags)
- [Playwright fake video capture — Microsoft issue #4532](https://github.com/microsoft/playwright/issues/4532)
- [Y4M webcam injection — Medium walkthrough](https://medium.com/@bshet768/dynamically-inject-webcam-data-in-playwright-tests-using-y4m-d96fdea2545c)
- [Fake video capture with Playwright — David Dal Busco](https://betterprogramming.pub/fake-video-capture-with-playwright-9314e6380755)
